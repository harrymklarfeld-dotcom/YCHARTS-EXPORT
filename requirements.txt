# core toolkit is stdlib-only; these add live data
robin_stocks>=3.4.0      # Robinhood private API (portfolio.robinhood_sync)
pyotp>=2.9               # only if your Robinhood login uses an authenticator app
yfinance>=0.2.50         # daily prices for the backtester / CSV import pricing
openpyxl>=3.1            # YCharts .xlsx exports
